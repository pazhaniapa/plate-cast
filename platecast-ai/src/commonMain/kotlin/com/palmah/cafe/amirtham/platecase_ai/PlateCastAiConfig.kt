package com.palmah.cafe.amirtham.platecase_ai

/**
 * Configuration for [PlateCastAiClient].
 *
 * [GEMINI_API_KEY] is a placeholder. Do not hardcode a real Gemini API key here or commit one
 * to version control — read it from a git-ignored local source (e.g. a `local.properties`
 * value wired through Gradle on Android/desktop, an untracked `.xcconfig` on iOS, or an
 * environment variable) and supply it at app startup instead.
 */
object PlateCastAiConfig {
    const val GEMINI_API_KEY: String = "<API KEY HERE>"
}
